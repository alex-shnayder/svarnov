@import '#/src/config';
@import '#/src/entities';

.SpecialOffers {
  @extend %component;
}

.offer {
  @extend %element;
  display: inline-block;
  width: 300px;
  height: 240px;
  vertical-align: top;

  & + & {
    margin-left: 30px;
  }
}
